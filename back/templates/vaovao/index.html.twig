{% extends 'base.html.twig' %}

{% block title %}Vaovao{% endblock %}

{% block body %}
    <h1>Vaovao</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Laharana</th>
                <th>Lohateny</th>
                <th>Votohatiny</th>
                <th></th>
            </tr>
        </thead>
        <tbody>
        {% for vaovao in vaovaos %}
            <tr>
                <td>{{ vaovao.idVaovao }}</td>
                <td>{{ vaovao.titre }}</td>
                <td>{{ vaovao.contenu }}</td>
                <td>
                    <a href="{{ path('vaovao_show', {'idVaovao': vaovao.idVaovao}) }}">Ijery</a>
                    <a href="{{ path('vaovao_edit', {'idVaovao': vaovao.idVaovao}) }}">Anova</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('vaovao_new') }}">Vaovao</a>
{% endblock %}
